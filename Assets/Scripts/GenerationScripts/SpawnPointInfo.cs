using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SpawnPointInfo : MonoBehaviour
{
    public SpawnPoint side;

    //public SpawnPointInfo spawnPoint;

    //public bool spawned = false;

    //public void Update()
    //{
    //    if (spawnPoint.transform.position == this.transform.position && side != SpawnPoint.NULL && spawnPoint.spawned == true)
    //    {
    //        side = SpawnPoint.NULL;
    //    }
    //}
}
